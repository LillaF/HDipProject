@model IEnumerable<FinalMerchBuild.Models.Bay>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Section.SectionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SectionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BayName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BayWidth)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Section.SectionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SectionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BayName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BayWidth)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BayID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BayID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BayID })
        </td>
    </tr>
}

</table>

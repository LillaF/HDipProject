@model IEnumerable<FinalMerchBuild.Models.Fixture>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Position.UPC)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Section.SectionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Position.UPC)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Section.SectionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FixtureID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FixtureID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FixtureID })
        </td>
    </tr>
}

</table>
